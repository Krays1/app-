<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black">

    <!-- Main vertical content: Score bar, board, controls (D-pad only) -->
    <LinearLayout
        android:id="@+id/mainContent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Score/Time/High Score bar at the very top -->
        <LinearLayout
            android:id="@+id/scoreBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:background="@drawable/header_background"
            android:padding="8dp">

            <TextView
                android:id="@+id/scoreText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Score: 0"
                android:textColor="@color/white"
                android:textSize="14sp"
                android:layout_marginEnd="16dp" />
            <TextView
                android:id="@+id/timeText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Time: 0s"
                android:textColor="@color/white"
                android:textSize="14sp"
                android:layout_marginEnd="16dp" />
            <TextView
                android:id="@+id/highScoreText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="High Score: 0"
                android:textColor="@color/white"
                android:textSize="14sp" />
        </LinearLayout>

        <!-- Game Board: perfect square, fills width, immediately below score bar -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">
            <FrameLayout
                android:id="@+id/snakeGameBoard"
                android:layout_width="0dp"
                android:layout_height="0dp"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintDimensionRatio="1:1" />
        </androidx.constraintlayout.widget.ConstraintLayout>

        <!-- Controls: D-pad and New Game/Pause, stacked vertically, always directly below the board -->
        <LinearLayout
            android:id="@+id/controlsLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center"
            android:paddingTop="0dp"
            android:paddingBottom="0dp"
            android:layout_marginTop="0dp"
            android:layout_marginBottom="0dp">

            <!-- D-Pad Controls -->
            <LinearLayout
                android:id="@+id/dpadLayout"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:gravity="center"
                android:layout_marginTop="0dp"
                android:layout_marginBottom="0dp">
                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center">
                    <View android:layout_width="64dp" android:layout_height="64dp" />
                    <Button android:id="@+id/btnUp" android:layout_width="64dp" android:layout_height="64dp" android:text="▲" android:textSize="28sp" android:background="@drawable/circle_button_red_selector" android:textColor="@android:color/white" android:fontFamily="sans-serif-bold" android:stateListAnimator="@null" style="@style/RedButtonStyle" />
                    <View android:layout_width="64dp" android:layout_height="64dp" />
                </LinearLayout>
                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center">
                    <Button android:id="@+id/btnLeft" android:layout_width="64dp" android:layout_height="64dp" android:text="◀" android:textSize="28sp" android:background="@drawable/circle_button_red_selector" android:textColor="@android:color/white" android:fontFamily="sans-serif-bold" android:stateListAnimator="@null" style="@style/RedButtonStyle" />
                    <View android:layout_width="64dp" android:layout_height="64dp" />
                    <Button android:id="@+id/btnRight" android:layout_width="64dp" android:layout_height="64dp" android:text="▶" android:textSize="28sp" android:background="@drawable/circle_button_red_selector" android:textColor="@android:color/white" android:fontFamily="sans-serif-bold" android:stateListAnimator="@null" style="@style/RedButtonStyle" />
                </LinearLayout>
                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center">
                    <View android:layout_width="64dp" android:layout_height="64dp" />
                    <Button android:id="@+id/btnDown" android:layout_width="64dp" android:layout_height="64dp" android:text="▼" android:textSize="28sp" android:background="@drawable/circle_button_red_selector" android:textColor="@android:color/white" android:fontFamily="sans-serif-bold" android:stateListAnimator="@null" style="@style/RedButtonStyle" />
                    <View android:layout_width="64dp" android:layout_height="64dp" />
                </LinearLayout>
            </LinearLayout>

            <!-- Button bar: New Game and Pause, centered below D-pad -->
            <LinearLayout
                android:id="@+id/buttonBar"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center"
                android:layout_marginTop="0dp"
                android:layout_marginBottom="0dp">
                <Button
                    android:id="@+id/btnNewGame"
                    android:layout_width="0dp"
                    android:layout_height="40dp"
                    android:layout_weight="1"
                    android:text="New Game"
                    android:textAllCaps="false"
                    android:textSize="14sp"
                    android:background="@drawable/circle_button_red_selector"
                    android:textColor="@android:color/white"
                    android:stateListAnimator="@null"
                    style="@style/RedButtonStyle" />
                <Space
                    android:layout_width="12dp"
                    android:layout_height="match_parent" />
                <Button
                    android:id="@+id/btnPauseResume"
                    android:layout_width="0dp"
                    android:layout_height="40dp"
                    android:layout_weight="1"
                    android:text="Pause"
                    android:textAllCaps="false"
                    android:textSize="14sp"
                    android:background="@drawable/circle_button_red_selector"
                    android:textColor="@android:color/white"
                    android:stateListAnimator="@null"
                    style="@style/RedButtonStyle" />
            </LinearLayout>
        </LinearLayout>
    </LinearLayout>

    <!-- Bottom Sheet Scoreboard (must be direct child of CoordinatorLayout) -->
    <LinearLayout
        android:id="@+id/scoreboardBottomSheet"
        android:layout_width="match_parent"
        android:layout_height="400dp"
        android:orientation="vertical"
        android:background="@drawable/header_background"
        android:elevation="8dp"
        android:padding="12dp"
        app:layout_behavior="com.google.android.material.bottomsheet.BottomSheetBehavior">

        <!-- Drag handle -->
        <View
            android:layout_width="40dp"
            android:layout_height="6dp"
            android:layout_gravity="center_horizontal"
            android:background="@android:color/darker_gray"
            android:layout_marginBottom="8dp"
            android:backgroundTint="@android:color/darker_gray"
            android:alpha="0.5" />

        <TextView
            android:id="@+id/leaderboardTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Scoreboard"
            android:textColor="@color/white"
            android:textSize="18sp"
            android:textStyle="bold"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="4dp" />
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/leaderboardRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="@android:color/transparent"
            android:overScrollMode="always"
            android:scrollbars="vertical" />
    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout> 